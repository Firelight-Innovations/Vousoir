# Markdown Math

**Notice:** This extension is bundled with Vousoir. It can be disabled but not uninstalled.

Adds math rendering using [KaTeX](https://katex.org) to Vousoir's built-in markdown preview and markdown cells in notebooks.
