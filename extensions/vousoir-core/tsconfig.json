{
	"//": "Extends the Vousoir strict base (work order \u00a77.2 scope explicitly covers extensions/vousoir-*), while pulling the vscode API types the way every code-oss built-in extension does \u2014 by including src/vscode-dts/vscode.d.ts rather than depending on @types/vscode (which is not published past 1.125.0). That include is an ambient TYPE reference, not a runtime import of core, so it does not breach \u00a77.1.",
	"extends": "../../vousoir/tsconfig.base.json",
	"compilerOptions": {
		"rootDir": "./src",
		"outDir": "./dist",
		"types": [
			"node"
		],
		"noEmit": true
	},
	"include": [
		"src/**/*.ts",
		"../../src/vscode-dts/vscode.d.ts"
	],
	"exclude": [
		"src/**/*.smoke.test.ts",
		"src/webview-harness.ts"
	]
}
