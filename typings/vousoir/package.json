{
  "name": "@vousoir/typings",
  "version": "0.0.0",
  "private": true,
  "description": "The single home for all Vousoir cross-package type declarations, enums-as-const, and zod schemas. Schemas are the one allowed runtime content: an MCP tool contract's types derive from its schema, so the schema is the contract.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
